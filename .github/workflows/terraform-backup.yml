name: terraform-backup

on:
  push:
    branches: [main]
    paths:
      - "terraform/live/backup/**"
      - "terraform/modules/backup-policy/**"
  pull_request:
    branches: [main]
    paths:
      - "terraform/live/backup/**"
      - "terraform/modules/backup-policy/**"

permissions:
  id-token: write
  contents: read

env:
  TF_WORKING_DIR: terraform/live/backup
  AWS_REGION: us-east-1

jobs:
  terraform:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'production' || 'preview' }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (bootstrap)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::247338422836:role/cicd-bootstrap
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-oidc-bootstrap

      - name: Assume terraform-execution role
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::247338422836:role/terraform-execution
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: terraform-backup
          role-chaining: true

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.8"

      - name: Terraform Init
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform init

      - name: Terraform Validate
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform validate

      - name: Terraform Plan
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform apply -auto-approve tfplan
